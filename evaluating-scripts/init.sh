# install allennlp
pip install allennlp

# Add the python lib to python's path, e.g.
# echo /path/to/allennlp-lib >> virtual_env/lib/python/site-packages/mpath.pth
# so that the custom allennlp script can be used
